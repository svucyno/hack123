# Smart Farmer SVU

Initial monorepo scaffold based on the reference project structure, using the same core technologies:

- `apps/web`: Next.js + React + TypeScript
- `apps/api`: NestJS + TypeScript
- `apps/expo`: Expo + React Native + TypeScript
